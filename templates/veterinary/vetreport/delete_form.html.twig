<form method="post" action="{{ path('vet_vetreport_delete', {'id': vetreport.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ vetreport.id) }}">
    <button class="btn">Delete</button>
</form>
