<form method="post" action="{{ path('vet_habitatreport_delete', {'id': habitatreport.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ habitatreport.id) }}">
    <button class="btn">Delete</button>
</form>
